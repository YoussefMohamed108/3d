[functions.create-order]
# Guest checkout is allowed, so we cannot require a valid JWT on every call.
# The function must validate all trusted values against server-side data.
verify_jwt = false

[functions.create-custom-order]
# Public commission requests are validated, rate-limited, and inserted by the
# function. Direct browser inserts are revoked by the accompanying migration.
verify_jwt = false
